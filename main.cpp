#include <iostream>
#include "opaque.hpp"

int main() {

    OPAQUE_INIT();


    std::cout << "[+] Opaque v2 (Obfuscated) is active." << std::endl;
    std::cout << "[+] CPU Registers & Timing checks passed." << std::endl;
    std::cout << "[+] Welcome to the underground, thyrn90. Core logic executing..." << std::endl;
    

    std::cin.get();
    return 0;
}